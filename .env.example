# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL="https://your-project-id.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"

# Server-Side Configuration (For API Route Webhooks)
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Email Trigger Configuration
RESEND_API_KEY="your-resend-api-key"

# ---------------------------------------------------------
# ZERO-KNOWLEDGE CONSTRAINTS:
# 1. Never store AES encryption passphrases in the `.env` or any server configuration.
# 2. Never attempt to log or persist the derived `CryptoKey`.
# 3. Encryption runs entirely client-side.
# ---------------------------------------------------------
